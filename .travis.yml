language: php
cache: false

sudo: false

php:
  - 7.4snapshot

env:
  matrix:
    - DEPENDENCIES="high"

before_script:
  - sh .travis.install.sh

script:
  - phpdbg -qrr vendor/bin/phpunit --testsuite=unit

matrix:
  allow_failures:
    - php: nightly
